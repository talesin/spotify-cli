# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a Spotify CLI application serving as a "proving ground to test different languages and frameworks by connecting to Spotify." The project is in its early stages with a basic repository structure.

## Project Structure

- `ts-effect/`: Directory for TypeScript Effect-TS implementation (currently empty)
- `README.md`: Basic project description
- `.gitignore`: Standard Node.js/TypeScript gitignore configuration
- LICENSE: Project licensing information

## Development Status

The project is currently in initialization phase with no source code, package.json, or build configuration present. The repository contains only basic scaffolding files and an empty `ts-effect` directory indicating future TypeScript/Effect-TS development.

## Architecture Notes

Based on the directory structure and repository name, this appears to be designed as:
- A CLI application for Spotify integration
- A testing ground for different programming languages and frameworks
- Currently focused on TypeScript with Effect-TS patterns (based on ts-effect directory)

## Claude Code Configuration

The repository includes Claude Code settings in `.claude/settings.local.json` allowing the use of `find` commands for file searching.

## Next Steps for Development

When implementing this project:
1. Initialize the appropriate language/framework in the designated directories
2. Set up build tools, package managers, and configuration files
3. Implement Spotify API integration
4. Add CLI interface and commands